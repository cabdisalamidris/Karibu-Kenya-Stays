# Local development

Start the API from `backend/` with `pipenv install` followed by `pipenv run flask --app run.py run --debug`.

In another terminal, start the client with `cd client`, `npm install`, and `npm run dev`. The first API start creates the SQLite database and seeds the demonstration catalogue.
