# Environment settings

`DATABASE_URL` selects the database and defaults to a local SQLite file. `JWT_SECRET_KEY` must be a strong private value in production. `JWT_ACCESS_TOKEN_HOURS` defaults to eight.

Set `FRONTEND_URL` to one or more comma-separated exact browser origins. Set `VITE_API_URL` in the Vercel client to the public Render API URL.
