# Verification checklist

Run `npm run build` from `client/` to verify the production frontend bundle. Start the API and check `GET /api/health`, then register a traveller, create a future stay and confirm it appears in My trips.

Sign in with the local demo administrator to check the dashboard, hotel creation and rating update flow. Test cancellation with a future reservation and verify that its room count is restored.
