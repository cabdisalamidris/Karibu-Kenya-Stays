# Contribution guidelines

Keep every change focused, run relevant verification before committing, and use a descriptive imperative commit subject. Avoid mixing implementation, deployment configuration and documentation in one commit.

Do not commit build output, local databases, secrets or dependencies. Update the relevant API or deployment note whenever behaviour changes.
